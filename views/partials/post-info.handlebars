    <article class="post">
      <div class="title">
        <h2>{{title}}</h2>
      </div>
      <div class="meta">
        <p>{{format_date created_at}}</p>
        <a href="/post/{{id}}">{{comments.length}} {{format_plural 'comment' comments.length}}</a>
      </div>
    </article>